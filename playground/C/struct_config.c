/**
 * using a struct to store settings to a device
 */
#include <stdio.h>

typedef enum Mode { 
    SNAP_SHOT,
    CONTINOUS
    };

typedef struct {
    Mode _mode;
} CameraSettingsConfig;

int main(
	
	return 0;
}
